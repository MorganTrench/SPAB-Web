import { MapExtrasDirective } from './map-extras.directive';

describe('MapExtrasDirective', () => {
  it('should create an instance', () => {
    const directive = new MapExtrasDirective();
    expect(directive).toBeTruthy();
  });
});
